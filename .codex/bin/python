#!/bin/sh
if [ -z "$GUARD_REAL_PYTHON" ]; then
  echo "GUARD_REAL_PYTHON is required for the guarded python wrapper." >&2
  exit 126
fi
"$GUARD_REAL_PYTHON" -B .codex/bin/guarded_command.py python "$@"
