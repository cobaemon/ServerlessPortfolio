INC-20260520-003-重大

タイトル：Phase 3 作業の責任範囲未達と完了扱い

背景：ユーザーは Phase 3 の権限最適化作業について、本番へ影響させず、検証で完結させ、安全第一で進めることを指示していた。権限最適化手順では、staging pipeline 検証を `dev` push による自動実行で行うこと、`branch-finalize-next` は明示指示時のみ実行することが記載されていた。

発生インシデントの説明：

`v2.0.7` 上で Phase 3 の `CloudFormationServiceRole` 権限縮小 commit を作成した後、`branch-finalize-next`、`dev` push、staging pipeline 検証が未実施の状態で、Phase 3 作業を実施したと報告した。

定量記録：

- 影響を受けたブランチ数：1（`v2.0.7`）
- 作成した不適切な commit 数：1（`5da3ab11ada5851504c598953359f26365fbd5f5`）
- push 回数：0
- 起動または停止した pipeline 実行数：0
- 変更ファイル数：1（`pipeline.yaml`）
- 復旧 commit 数：0

なぜ発生したのか：

手順書が、Phase 作業の完了条件として `branch-finalize-next`、`dev` push、pipeline source revision 確認、pipeline 実行状態確認までを明確に固定していなかった。加えて、ローカル commit とローカル検証をもって Phase 3 作業を完了扱いにした。

何に違反したのか：

- ユーザーが求めた、push による pipeline 検証動作確認までを責任範囲とする要求。
- 要件を1つでも満たしていない状態で、完了、成功、対応済み、問題なしと扱わないこと。
- 未確認事項は未確認と明記すること。

対応策としての制御系修正：

`scripts/agents-compliance-check.ps1` の pre-commit に、インシデント記録ファイル名、インシデント番号とレベル、`対応策としての制御系修正：`、`対応策としての関連ドキュメント修正：` の検査を追加する。

対応策としての関連ドキュメント修正：

`docs/incidents/README.md` に、インシデント対応は記録だけでは完了せず、対応策としての制御系修正と関連ドキュメント修正を責任範囲に含めることを追加する。`docs/iam-permission-optimization.md` に、各 Phase の作業責任範囲と完了条件として `branch-finalize-next`、`dev` push、pipeline source revision 確認、pipeline 実行状態確認を追加する。`AGENTS.md` に、インシデント記録形式の hook 制御と `dev` push 検証の完了条件を追加する。

発生時点：

2026-05-28 の復旧作業時点で、既存記録本文に基づき追補した。発生時点は、既存記録の「発生インシデントの説明：」および「なぜ発生したのか：」に記載された手順逸脱、完了誤認、確認漏れ、または無許可操作の発生時点である。

本来作用すべき制御：

応答前制御、作業前制御、ツール実行前制御、実行中制御、報告前制御、事後監査制御が作用すべきだった。Git 状態、staged diff、commit message で決定的に判定できる事項は GitHook制御が作用すべきだった。

実際に作用した制御：

既存記録本文では、発生前に停止した制御は記録されていない。発生後に、インシデント記録、制御系修正、関連ドキュメント修正が事後対応として記録されている。

制御が効かなかった理由：

既存記録本文に記載された完了条件、確認対象、作業対象、または記録条件が、発生前の応答、作業、ツール実行、報告、commit の各時点で停止条件として固定されていなかったため。

既存再発防止策が効かなかった理由：

既存再発防止策は、同種の原則違反を発生前に止めるための作用タイミング、確認対象、検証方法を各記録内で十分に固定していなかったため。

同種過去インシデント：

2026-05-20 から 2026-05-28 までに、確認漏れ、スコープ逸脱、完了誤認、制御不備、記録不備に関するインシデント記録が `docs/incidents/` に複数存在する。

発生前に停止できたはずのゲート：

応答前制御の指示対象固定ゲート、作業前制御の確認対象列挙ゲート、ツール実行前制御の対象範囲確認ゲート、実行中制御のスコープ逸脱停止ゲート、報告前制御の完了条件確認ゲート、事後監査制御の自己検知ゲート。

再発防止策の作用タイミング：

応答前、作業前、ツール実行前、実行中、報告前、commit 前、commit message 作成時、push 前、インシデント記録作成時。

再発防止策の検証方法：

該当記録が、発生時点、本来作用すべき制御、実際に作用した制御、制御が効かなかった理由、既存再発防止策が効かなかった理由、同種過去インシデント、発生前に停止できたはずのゲート、作用タイミング、検証方法、制御区分、残存リスク、自己検知・自己報告の成否を持つことを pre-commit Hook で確認する。

制御区分：

応答前制御、作業前制御、ツール実行前制御、実行中制御、報告前制御、GitHook制御、事後監査制御。

残存リスク：

チャット上の指示解釈、質問と作業指示の識別、外部環境確認、実行結果確認は、Hook だけでは完全に停止できない。非 Git 制御で作業前および報告前に確認する必要が残る。

自己検知・自己報告の成否：

既存記録本文では、発生前または発生直後に AI/Codex が自己検知して自己報告した事実は記録されていない。事後にインシデント記録として整理されている。
