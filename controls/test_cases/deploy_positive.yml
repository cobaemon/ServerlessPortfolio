{
  "version": 1,
  "category": "deploy",
  "cases": [
    {
      "id": "DP-001",
      "kind": "deploy_command",
      "input": "aws codepipeline get-pipeline-state --profile aws_portfolio_profile --region ap-northeast-1 --name cobaemon-serverless-portfolio-pipeline",
      "expected": "ALLOW",
      "deploy_allowed": true,
      "env": {"AWS_PROFILE": "aws_portfolio_profile", "AWS_REGION": "ap-northeast-1", "GUARD_SOURCE_REVISION": "0123456789abcdef"}
    }
  ]
}
